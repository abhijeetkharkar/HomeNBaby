# cinema-manager-models

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cinema-manager-models` to build the library.

## Running unit tests

Run `nx test cinema-manager-models` to execute the unit tests via [Jest](https://jestjs.io).
